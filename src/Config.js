var debug = true;
var backendUrlPrefix = (debug ? 'http://localhost:6253' : '');
var frontendUrlPrefix = (debug ? 'http://localhost:3010' : '');
var Config = {
    colors: {
        theme: '#bd1c1b',
        themeDarker: '#961a19'
    },
};

export default Config;